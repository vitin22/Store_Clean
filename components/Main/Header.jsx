import Search from '@components/Main/Search';
import TopHeader from './TopHeader';

const Header = () => {
	return(
		<header>
			<TopHeader/>
			<Search/>
		</header>
);
	};

export default Header;